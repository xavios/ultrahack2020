import React from "react";

export default class Profile extends React.Component {
    render() {
        return (
            <div className="alert alert-primary alert-warning" role="alert">
                This is the profile page!
            </div>
        );
    }
}