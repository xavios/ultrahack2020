import React, { FC, ReactElement } from "react";

const Home : FC = (): ReactElement => {    
    return (
        <div className="alert alert-primary alert-success" role="alert">
            This is the home page!
        </div>
    ) 
 };
 
 export default Home;